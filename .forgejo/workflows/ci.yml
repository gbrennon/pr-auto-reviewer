name: CI

on:
  push:
    branches:
      - main
      - release/**
  pull_request:
    branches:
      - main
      - release/**

permissions:
  contents: read

jobs:
  test:
    runs-on: codeberg-medium
    steps:
      - uses: actions/checkout@v6
